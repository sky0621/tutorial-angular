import { StrengthenPipe } from './strengthen.pipe';

describe('StrengthenPipe', () => {
  it('create an instance', () => {
    const pipe = new StrengthenPipe();
    expect(pipe).toBeTruthy();
  });
});
