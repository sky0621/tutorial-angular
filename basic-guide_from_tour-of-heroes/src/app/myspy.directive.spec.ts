import { MyspyDirective } from './myspy.directive';

describe('MyspyDirective', () => {
  it('should create an instance', () => {
    const directive = new MyspyDirective();
    expect(directive).toBeTruthy();
  });
});
